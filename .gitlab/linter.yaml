Lint:
  stage: linters
  script:
    - pip install virtualenv flake8
    - virtualenv venv
    - source venv/bin/activate
    - flake8 --color always
  tags:
    - ci-docker
  except:
    refs:
      - schedules
      - pipelines
  allow_failure: true